#include <stdio.h>
#include <stdio.h>

//Funcao generica para entrada de dados
void entrada_de_dados() {
    // código para entrada de dados
}

//Funcao generica calculo de media
float calcular_media() {
    //codigo para a funcao calcular media 
    return 0.0; // valor de retorno temporário
}

//Funcao  generica exibir dados
void exibir_media(float media) {
    // código para exibir a média
    printf("Media: %.2f\n", media);
}

int main() {
    entrada_de_dados();
    float media = calcular_media();
    exibir_media(media);
    return 0;
}
